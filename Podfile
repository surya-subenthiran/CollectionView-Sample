# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CollectionView_Example_Universal' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  target 'CollectionView_Example_UniversalTests' do
    inherit! :search_paths
  end

  target 'CollectionView_Example_UniversalUITests' do
    inherit! :search_paths
  end

end
